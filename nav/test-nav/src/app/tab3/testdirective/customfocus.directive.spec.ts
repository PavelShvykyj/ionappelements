import { CustomfocusDirective } from './customfocus.directive';

describe('CustomfocusDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomfocusDirective();
    expect(directive).toBeTruthy();
  });
});
